<%= form_for [cd, comment] do |f| %>
	<div>
	<%= f.label :comment_body, "コメント内容" %>
  	<%= f.text_field :comment_body, class: "form-control" %>
  	<%= f.hidden_field :cd_id, value: comment.id %>
  	</div>
  	<%= f.submit  class: "btn btn-default" %>
<% end %>
</div>